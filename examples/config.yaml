$schema: "../schema/config.json"

issuers:
  chickens:
    key:
    scopes:
      - openid
      - foo
    clients:
      - confidential:
          id: client-id
          secret: client-secret
      - public:
          id: frontend
          redirectUrls:
            - http://localhost
